// Translation namespaces used by _app and the layout (sidebar, navigation, cookie banner) on every page;
// each page adds its own so the prerendered HTML only carries the strings that page renders
const LAYOUT_NAMESPACES = ['common', 'seo', 'sidebar', 'navigation'];

export default LAYOUT_NAMESPACES;
